module.exports = (api) => {
  /*
  api.chainWebpack(config => {
    config.module
      .rule('uikit')
        .test(require.resolve('uikit'))
        .use('expose')
          .loader('expose-loader')
          .options('UIkit')
          .end()
  })
  */
}
